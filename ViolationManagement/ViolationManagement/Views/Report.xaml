<Window x:Class="ViolationManagement.Views.Report"
            xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
            xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
            Title="Phản ánh vi phạm" Height="700" Width="900"
            WindowStartupLocation="CenterScreen"
            Background="#F5F5F5">

    <Window.Resources>
        <!-- Styles -->
        <Style x:Key="ModernTextBox" TargetType="TextBox">
            <Setter Property="Padding" Value="12,10"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="BorderBrush" Value="#E0E0E0"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Background" Value="White"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border Background="{TemplateBinding Background}"
                                    BorderBrush="{TemplateBinding BorderBrush}"
                                    BorderThickness="{TemplateBinding BorderThickness}"
                                    CornerRadius="6">
                            <ScrollViewer x:Name="PART_ContentHost"
                                            Margin="{TemplateBinding Padding}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsFocused" Value="True">
                                <Setter Property="BorderBrush" Value="#1E88E5"/>
                                <Setter Property="BorderThickness" Value="2"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="BorderBrush" Value="#BDBDBD"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="ModernButton" TargetType="Button">
            <Setter Property="Padding" Value="20,10"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="Medium"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                    CornerRadius="6"
                                    Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center"
                                                VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Opacity" Value="0.9"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Opacity" Value="0.8"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="LabelStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="Medium"/>
            <Setter Property="Foreground" Value="#424242"/>
            <Setter Property="Margin" Value="0,15,0,8"/>
        </Style>

        <Style x:Key="NavButtonStyle" TargetType="Button" BasedOn="{StaticResource ModernButton}">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Padding" Value="15,8"/>
            <Setter Property="FontSize" Value="13"/>
        </Style>
    </Window.Resources>

    <DockPanel LastChildFill="True">
        <!-- Header -->
        <Grid DockPanel.Dock="Top" Height="60">
            <Grid.Background>
                <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                    <GradientStop Color="#D32F2F" Offset="0"/>
                    <GradientStop Color="#B71C1C" Offset="1"/>
                </LinearGradientBrush>
            </Grid.Background>

            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <!-- Logo Section -->
            <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="20,0,0,0">
                <Border Background="White" CornerRadius="20" Width="35" Height="35" Margin="0,0,15,0">
                    <TextBlock Text="🚨" FontSize="18" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                </Border>
                <StackPanel VerticalAlignment="Center">
                    <TextBlock Text="Báo cáo vi phạm giao thông"
                                   Foreground="White"
                                   FontWeight="Bold"
                                   FontSize="18"/>
                    <TextBlock Text="Hệ thống quản lý vi phạm"
                                   Foreground="#FFCDD2"
                                   FontSize="11"/>
                </StackPanel>
            </StackPanel>

            <!-- Navigation Buttons -->
            <StackPanel Orientation="Horizontal" Grid.Column="2"
                            HorizontalAlignment="Right" VerticalAlignment="Center" Margin="0,0,20,0">
                <Button x:Name="btnHome" Content="🏠 Trang chủ" Click="OpenHome" Style="{StaticResource NavButtonStyle}" Margin="5,0"/>
                <Button x:Name="btnLogout" Content="🚪 Đăng xuất" Click="Logout" Style="{StaticResource NavButtonStyle}" Margin="5,0"/>
            </StackPanel>
        </Grid>

        <!-- Main Content -->
        <Grid Margin="0">
            <ScrollViewer VerticalScrollBarVisibility="Auto" Padding="40,30">
                <Border Background="White" CornerRadius="12" Padding="40">
                    <Border.Effect>
                        <DropShadowEffect Color="Black" Opacity="0.1" BlurRadius="20" ShadowDepth="2"/>
                    </Border.Effect>

                    <StackPanel MaxWidth="600">
                        <!-- Header -->
                        <StackPanel Orientation="Horizontal" Margin="0,0,0,30">
                            <Border Background="#E3F2FD" CornerRadius="25" Width="50" Height="50" Margin="0,0,20,0">
                                <TextBlock Text="📢" FontSize="24" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                            <StackPanel VerticalAlignment="Center">
                                <TextBlock Text="Gửi phản ánh vi phạm giao thông"
                                               FontSize="24" FontWeight="Bold" Foreground="#1565C0"/>
                                <TextBlock Text="Vui lòng điền đầy đủ thông tin bên dưới"
                                               FontSize="14" Foreground="#757575" Margin="0,5,0,0"/>
                            </StackPanel>
                        </StackPanel>

                        <!-- Form Fields -->
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="20"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>

                            <StackPanel Grid.Column="0">
                                <TextBlock Text="Loại vi phạm *" Style="{StaticResource LabelStyle}"/>
                                <TextBox x:Name="ViolationTypeTextBox" Style="{StaticResource ModernTextBox}"
                                             ToolTip="Ví dụ: Vượt đèn đỏ, Đỗ xe sai quy định..."/>

                                <TextBlock Text="Biển số xe *" Style="{StaticResource LabelStyle}"/>
                                <TextBox x:Name="PlateNumberTextBox" Style="{StaticResource ModernTextBox}"
                                             ToolTip="Ví dụ: 30A-12345"/>
                            </StackPanel>

                            <StackPanel Grid.Column="2">
                                <TextBlock Text="Địa điểm *" Style="{StaticResource LabelStyle}"/>
                                <TextBox x:Name="LocationTextBox" Style="{StaticResource ModernTextBox}"
                                             ToolTip="Địa chỉ cụ thể nơi xảy ra vi phạm"/>
                            </StackPanel>
                        </Grid>

                        <TextBlock Text="Mô tả chi tiết *" Style="{StaticResource LabelStyle}" Margin="0,25,0,8"/>
                        <TextBox x:Name="DescriptionTextBox" Style="{StaticResource ModernTextBox}"
                                     Height="100" AcceptsReturn="True" TextWrapping="Wrap"
                                     ToolTip="Mô tả chi tiết về hành vi vi phạm"/>

                        <!-- File Upload Section -->
                        <Border Background="#F8F9FA" CornerRadius="8" Padding="20" Margin="0,25,0,0">
                            <StackPanel>
                                <TextBlock Text="📎 Tệp đính kèm" FontSize="16" FontWeight="Bold" 
                                               Foreground="#495057" Margin="0,0,0,15"/>

                                <Grid>
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="Auto"/>
                                    </Grid.RowDefinitions>

                                    <!-- Image Upload -->
                                    <Border Background="White" CornerRadius="6" Padding="15" Margin="0,0,0,15">
                                        <StackPanel>
                                            <TextBlock Text="📷 Ảnh vi phạm" FontWeight="Medium" Margin="0,0,0,10"/>
                                            <Grid>
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="*"/>
                                                    <ColumnDefinition Width="Auto"/>
                                                </Grid.ColumnDefinitions>
                                                <TextBox x:Name="ImagePathBox" Style="{StaticResource ModernTextBox}"
                                                             IsReadOnly="True" Background="#F8F9FA"
                                                             Text="Chưa chọn file..."/>
                                                <Button Grid.Column="1" Content="Chọn ảnh" Click="SelectImage"
                                                            Style="{StaticResource ModernButton}"
                                                            Background="#28A745" Foreground="White"
                                                            Margin="10,0,0,0"/>
                                            </Grid>
                                        </StackPanel>
                                    </Border>

                                    <!-- Video Upload -->
                                    <Border Grid.Row="1" Background="White" CornerRadius="6" Padding="15">
                                        <StackPanel>
                                            <TextBlock Text="🎥 Video vi phạm" FontWeight="Medium" Margin="0,0,0,10"/>
                                            <Grid>
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="*"/>
                                                    <ColumnDefinition Width="Auto"/>
                                                </Grid.ColumnDefinitions>
                                                <TextBox x:Name="VideoPathBox" Style="{StaticResource ModernTextBox}"
                                                             IsReadOnly="True" Background="#F8F9FA"
                                                             Text="Chưa chọn file..."/>
                                                <Button Grid.Column="1" Content="Chọn video" Click="SelectVideo"
                                                            Style="{StaticResource ModernButton}"
                                                            Background="#6C757D" Foreground="White"
                                                            Margin="10,0,0,0"/>
                                            </Grid>
                                        </StackPanel>
                                    </Border>
                                </Grid>
                            </StackPanel>
                        </Border>

                        <!-- Submit Button -->
                        <Button Content="📨 Gửi phản ánh" Height="50" Margin="0,30,0,0"
                                    Style="{StaticResource ModernButton}"
                                    Background="#1E88E5" Foreground="White"
                                    FontSize="16" FontWeight="Bold"
                                    Click="SubmitReport">
                            <Button.Effect>
                                <DropShadowEffect Color="#1E88E5" Opacity="0.3" BlurRadius="10" ShadowDepth="2"/>
                            </Button.Effect>
                        </Button>

                        <!-- Footer Note -->
                        <TextBlock Text="* Các trường bắt buộc phải điền"
                                       FontSize="12" Foreground="#9E9E9E"
                                       HorizontalAlignment="Center" Margin="0,20,0,0"/>
                    </StackPanel>
                </Border>
            </ScrollViewer>
        </Grid>
    </DockPanel>
</Window>